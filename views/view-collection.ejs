<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Cards</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">
            <img class="logo" src="<%=logoPath %>" alt="logo">
          </a>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link" aria-current="page" href="home">Home</a>
              <a class="nav-link"  href="view-cards">Browse Cards</a>
              <a class="nav-link" href="#">Browse Collections</a>
              <a class="nav-link active" href="view-collection">View My Collection</a>
              <a class="nav-link" href="sign-up">Create an Account</a>
              <a class="nav-link" href="login">Login to Account</a>
    
            </div>
          </div>
        </div>
      </nav>

  <h1><%=username %>'s collection!</h1>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Image</th>
        <th>HP</th>
        <th>Stage</th>
        <th>Attack</th>
        <th>Rarity</th>
        <th>Weakness</th>
        <th>Set Name</th>
        <th>Evolves From</th>
      </tr>
    </thead>
    <tbody>
      <% cards.forEach(card=> { %>
        <tr>
          <td>
            <%= card.pokemon_name %>
          </td>
          <td>
            <%= card.pokemon_type_name %>
          </td>
          <td><img src="<%= card.url_img %>" alt="<%= card.pokemon_name %>" width="125"></td>
          <td>
            <%= card.hp %>
          </td>
          <td>
            <%= card.pokemon_stage_name %>
          </td>
          <td>
            <%= card.attack %>
          </td>
          <td>
            <%= card.rarity %>
          </td>
          <td>
            <%= card.weakness %>
          </td>
          <td>
            <%= card.pokemon_set_name %>
          </td>
          <td>
            <%= card.evolve_from || '-' %>
          </td>
          <td><button type="button" class="btn btn-secondary">Favourite</button></td>
          <td><button type="button" class="btn btn-secondary">Remove</button></td>
        </tr>
        <% }); %>
    </tbody>
  </table>

<!-- <div class="container">
  
  <div class="row pb-3">
    <% cards.forEach(card=> { %>
<div class="column p-3 w-50">
  <img src="<%= card.url_img %>" alt="<%= card.pokemon_name %>" >
</div>
<div class="column p-3">
  <%= card.pokemon_name %>
</div>
<% }); %>
  </div>
  
</div>

   -->
</body>

</html>